
name: Eleventy Build

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
    - name: Eleventy Build
      uses: TartanLlama/actions-eleventy@v1.2
            
